{
  "name": "curate-bids",
  "label": "BIDS Curation",
  "description": "Use this gear to initialize BIDS filenames and attributes on all files within a given project.",
  "author": "Flywheel <support@flywheel.io>",
  "maintainer": "Flywheel <support@flywheel.io>",
  "source": "https://github.com/flywheel-apps/curate-bids",
  "url": "http://bids.neuroimaging.io/",
  "version": "1.1.1_0.9.3_rc4",
  "custom": {
    "gear-builder": {
      "category": "converter",
      "image": "flywheel/curate-bids:1.1.1_0.9.3_rc4"
    },
    "flywheel": {
      "suite": "BIDS"
    }
  },
  "license": "BSD-3-Clause",
  "inputs": {
    "api_key": {
      "base": "api-key"
    }
  },
  "config": {
    "reset": {
      "description": "Remove all BIDS info from files before curating",
      "type": "boolean",
      "default": false
    },
    "entire_project": {
      "description": "Run bids curation on the entire project.  If this is not selected then run on only the current session.",
      "type": "boolean",
      "default": true
    },
    "template_type": {
      "description": "Which template type to use. Options: Default, ReproIn, or Custom. Note: Custom expects a BIDS Project Curation JSON Template as a project-level file with the filename ending in project-template.json",
      "type": "string",
      "default": "Default",
      "enum": [
        "Default",
        "BIDS-v1",
        "ReproIn",
        "Custom"
      ]
    },
    "verbosity": {
      "description": "Set log level to INFO or DEBUG.",
      "type": "string",
      "default": "INFO",
      "enum": [
        "INFO",
        "DEBUG"
      ]
    }
  },
  "command": "python3 run.py"
}
